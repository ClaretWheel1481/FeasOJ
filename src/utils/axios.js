// TODO:每次编译前需要将其设置为你反向代理的地址，例如http://cloud.claret.space:38080/api/v1
export const apiUrl = "https://cloud.claret.space:37881/api/v1"
export const avatarServer = `${apiUrl}/avatar/`
export const docsServer = `${apiUrl}/docs/`
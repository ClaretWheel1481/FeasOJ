FROM ubuntu:latest

# 更新包列表并安装必要的软件
RUN apt-get update && apt-get install -y \
    build-essential \
    gcc \
    g++ \
    openjdk-17-jdk \
    golang \
    python3 \
    python3-pip

# 设置工作目录
WORKDIR /workspace

# 设置默认命令
CMD ["bash"]